{
  "name": "outdoor-deal-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "prepare:source": "node scripts/prepare-build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "build:next": "next build",
    "build": "pnpm prepare:source && pnpm validate:config && pnpm build:next",
    "ci": "pnpm run preflight",
    "start": "next start",
    "validate:config": "node scripts/validate-config.mjs",
    "preflight": "pnpm prepare:source && pnpm validate:config && pnpm typecheck && pnpm test && pnpm build:next"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "cheerio": "^1.0.0",
    "next": "^16.0.0",
    "p-limit": "^6.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "robots-parser": "^3.0.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  },
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": "24.x",
    "pnpm": "10.15.1"
  }
}
